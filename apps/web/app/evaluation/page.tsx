import PagePlaceholder from "@/components/PagePlaceholder";

export default function EvaluationPage() {
  return (
    <PagePlaceholder
      title="Evaluation"
      description="Evaluation metrics for agent outputs. No fake metrics are displayed — data arrives in Phase 5."
    />
  );
}
