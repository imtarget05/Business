import PagePlaceholder from "@/components/PagePlaceholder";

export default function DashboardPage() {
  return (
    <PagePlaceholder
      title="Overview"
      description="System-wide status: task throughput, agent health and recent activity. Live metrics arrive with Phase 1."
    />
  );
}
