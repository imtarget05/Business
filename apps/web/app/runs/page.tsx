import PagePlaceholder from "@/components/PagePlaceholder";

export default function RunsPage() {
  return (
    <PagePlaceholder
      title="Agent Runs"
      description="Per-agent execution timeline: attempts, durations, errors and retries."
    />
  );
}
