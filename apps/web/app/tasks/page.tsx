import PagePlaceholder from "@/components/PagePlaceholder";

export default function TasksPage() {
  return (
    <PagePlaceholder
      title="Tasks"
      description="Task lifecycle monitor: pending → classifying → routing → running → validating → terminal states."
    />
  );
}
