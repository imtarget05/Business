import PagePlaceholder from "@/components/PagePlaceholder";

export default function AuditPage() {
  return (
    <PagePlaceholder
      title="Audit Logs"
      description="Append-only audit trail of every task, delegation and system event."
    />
  );
}
